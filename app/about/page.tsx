import React from 'react';
import {Metadata} from "next";

export const metadata:Metadata={
    title:"About | Next Start"
}

const About = () => {
    return (
        <h3>
            Select item
        </h3>
    );
};

export default About;